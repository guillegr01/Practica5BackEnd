import { Collection } from "mongodb";
import { User, UserModel } from "./types.ts";

export const resolvers = {

    /*Query: {
        users: async (_:unknown, __:unknown, context: { UserCollection: Collection<UserModel> }): Promise<User[]> => {
            const UsersDB = await context.UserCollection.find().toArray();
            
        }
    },

    Mutation: {

    }

    User: {
        posts: (parent: UserModel): PostModel => {
            return ctx.postCollection.find
        }
    },
    */
    
};